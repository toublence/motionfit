모션 핏 - 스쿼트 스토어 스크린샷

목표

실제 앱 빌드의 기능과 UI를 근거로, 축소 썸네일에서도 메시지가 읽히는 스토어 이미지를 만든다. 판매력 있는 가독성과 메타데이터의 정확성을 동시에 만족시킨다.

다음 원칙은 디자인 선호보다 우선한다.

실제 앱에 존재하고 현재 빌드에서 재현되는 기능만 보여준다.

앱 화면, 사람, 포즈 라인, 수치, 상태 표시줄을 생성형 이미지로 만들거나 포토샵으로 조작하지 않는다.

iOS App Store용 이미지에는 iOS/iPadOS에서 직접 렌더링된 화면만 사용한다.

근거가 없거나 캡처가 불가능하면 이미지를 만들지 말고 누락된 자료를 보고한다.

고정 범위

프로젝트 루트는 /Users/nam/projects/squat만 사용한다.

지원 언어는 ko, en, ja, de, fr, es, ar 7개로 제한한다.

언어별로 iPhone과 iPad 세트를 각각 만든다.

기본 구성은 기기·언어별 6장이다.

최종 파일은 불투명 배경의 세로형 PNG 또는 App Store Connect가 허용하는 JPEG로 만든다.

iPad는 실제 iPad 레이아웃을 캡처한다. iPhone 화면을 확대하거나 비율만 바꿔 재사용하지 않는다.

App Store와 Google Play의 원본 캡처, 프레임, 상태 표시줄, 출력 폴더를 완전히 분리한다.

필수 산출물

이미지를 만들기 전에 다음 파일을 먼저 작성한다.

storyboard.md: 화면 순서, 헤드라인, 부제목, 사용할 실제 화면

evidence.md: 각 기능 주장과 구현·실행 근거의 대응표

capture-manifest.md: 빌드, 커밋, 기기, OS, 언어, 원본 캡처 파일, 촬영 방법

이미지 생성 후 다음 파일을 작성한다.

qa-report.md: 크기, 알파 채널, 텍스트, 현지화, 기기 일치, 상태 표시줄 검수

edit-log.md: 원본 캡처별로 적용한 작업을 기록한다. 허용 작업은 자르기, 균일 확대·축소, 마스킹, 배경·카피 배치로 제한한다.

실패 우선 원칙

다음 중 하나라도 충족하지 못하면 해당 장을 생성하지 않는다.

기능이 소스 코드와 실행 화면에서 모두 확인되지 않는다.

현재 배포 후보 빌드에서 해당 화면을 재현할 수 없다.

App Store용인데 실제 iPhone 또는 iPad 캡처가 없다.

카메라 화면인데 실제 앱이 렌더링한 사람·랜드마크·카운트·피드백 원본이 없다.

상태 표시줄이나 기기 프레임의 플랫폼·모델을 확정할 수 없다.

카피가 실제 화면보다 더 강한 기능을 주장한다.

누락을 생성형 이미지나 임의의 목업으로 보충하지 않는다.

실행 워크플로

1. 프로젝트와 빌드 확인

/Users/nam/projects/squat가 존재하는지 확인한다. 없으면 중단한다.

앱 타깃, 지원 기기, 지원 방향, 현지화 리소스, 카메라 권한 흐름을 확인한다.

스크린샷에 사용할 빌드 번호와 Git 커밋 SHA를 기록한다.

각 후보 기능을 소스 코드에서 찾고 실제 앱에서 실행해 확인한다.

구현 흔적만 있고 실행되지 않는 기능은 사용하지 않는다.

2. 기능 근거표 작성

evidence.md에 다음 열을 사용한다.

| 주장 | 소스 근거 | 실행 경로 | 실제 캡처 | 빌드/커밋 | 사용 가능 여부 |

예:

| 스쿼트 자동 카운트 | 파일·심볼 | 홈 > 운동 시작 | captures/ios/ko/iphone/count.png | 1.0.9/abc123 | 가능 |

“실시간”, “자동”, “음성”, “자세 안내”, “챌린지” 같은 단어는 실제 동작을 직접 확인한 경우에만 사용한다.

3. 원본 캡처 확보

iPhone용 원본은 현재 iOS 앱을 iPhone 시뮬레이터 또는 실제 iPhone에서 실행해 캡처한다.

iPad용 원본은 현재 iPadOS 앱을 iPad 시뮬레이터 또는 실제 iPad에서 실행해 캡처한다.

카메라 기반 운동 화면은 실제 기기에서 앱이 실제 입력을 처리하는 장면을 우선한다.

시뮬레이터 캡처는 xcrun simctl io <device> screenshot <file>처럼 운영체제가 직접 생성하는 방식을 사용한다.

카메라 피드, 사람, 포즈 랜드마크, 카운트, 경고, 음성 코칭 표시를 별도로 합성하지 않는다.

인물이 필요하면 촬영 동의를 받은 사람을 사용하고, 개인 식별 정보가 노출되지 않게 한다.

통계와 기록 수치는 앱의 실제 사용 또는 앱 내부의 정식 테스트 데이터 경로를 통해 만든다. 이미지 편집기로 숫자를 바꾸지 않는다.

4. 스토리보드 작성

기본 순서는 다음과 같다. 해당 기능이 실제로 확인된 경우에만 사용한다.

자동 카운트

실시간 자세 또는 음성 코칭

챌린지 또는 반복 사용 가치

세트와 휴식

운동 결과

기록

첫 3장에서 자동 카운트, 즉시 피드백, 반복 사용 가치를 우선한다. 확인되지 않은 기능이 있으면 실제 기능으로 교체하고 이유를 storyboard.md에 기록한다.

스토리보드 형식:

1. 자동 카운트
헤드라인: 스쿼트 자동 카운트
부제목: 운동에만 집중하세요
화면: captures/ios/ko/iphone/count.png
근거: evidence.md 항목 1
검수: 2줄 이하 / 실제 UI / 실제 수치 / 실제 iOS 캡처

5. 카피 작성

헤드라인은 2~6단어, 1~2줄로 작성한다.

부제목은 0~1줄로 작성한다.

한 장에 메시지 하나만 전달한다.

기능보다 사용자가 얻는 결과를 먼저 표현한다.

실제 화면에서 확인 가능한 내용만 말한다.

의료, 치료, 부상 예방, 확정적인 자세 교정 효과를 주장하지 않는다.

전문 트레이너나 의료 서비스를 대체한다고 말하지 않는다.

금지 표현:

쉽게 운동하세요

간편하게 관리하세요

건강해지세요

더 나은 삶

맞춤형 경험

스마트한 운동 관리

6. 레이아웃 합성

상단 메시지 영역은 전체 높이의 약 25%~35%를 사용한다.

앱 화면 영역은 약 55%~70%를 사용한다.

헤드라인이 가장 먼저 보이게 한다.

로고와 장식은 최소화한다.

앱 UI 원본은 비율을 유지해 균일하게 확대·축소한다.

앱 UI 내부의 텍스트, 버튼, 수치, 사람, 랜드마크를 다시 그리지 않는다.

생성형 이미지는 앱 화면과 제품 기능을 표현하는 데 사용하지 않는다.

배경에는 단색, 그라디언트, 단순한 비의미적 도형만 사용할 수 있다.

사람, 운동 장면, 기기, OS UI, 알림, 상태 표시줄을 배경 장식으로 생성하지 않는다.

App Store 정확성 규칙

실제 iOS/iPadOS 화면만 사용

App Store용 앱 화면은 같은 플랫폼의 현재 빌드 원본 캡처여야 한다.

Android 캡처, 웹 브라우저 캡처, 디자인 툴 목업, 생성형 UI를 사용하지 않는다.

iPhone 캡처를 iPad 프레임에 넣지 않는다.

iPad 캡처를 iPhone 프레임에 넣지 않는다.

다른 모바일 플랫폼의 이름, 아이콘, 내비게이션 바, 상태 표시줄, 마켓 이미지를 넣지 않는다.

상태 표시줄 게이트

App Store용 최종 이미지의 앱 화면에는 다음 두 방식만 허용한다.

같은 원본 iOS/iPadOS 캡처에 포함된 상태 표시줄을 픽셀 변경 없이 유지한다.

원본 앱 화면 전체를 일관되게 잘라 상태 표시줄을 전혀 보이지 않게 한다.

다음을 금지한다.

상태 표시줄을 직접 그리거나 생성한다.

시간, 날짜, Wi-Fi, 통신, 배터리 아이콘을 다른 이미지에서 가져와 붙인다.

Android 또는 정체불명의 상태 표시줄을 사용한다.

상태 표시줄 일부만 지운 뒤 배경을 생성형 채우기로 복원한다.

iPhone 상태 표시줄을 iPad 화면에 붙이거나 그 반대로 한다.

상태 표시줄을 정리해야 하면 시뮬레이터가 제공하는 시스템 기능으로 다시 캡처한다. 이미지 편집기로 시스템 UI를 제작하지 않는다.

기기 프레임 게이트

기본값은 외부 기기 목업을 사용하지 않는 것이다.

프레임이 꼭 필요하면 해당 캡처와 동일한 Apple 기기 모델의 공식 또는 사용 권한이 확인된 리소스만 사용한다.

모델을 확정할 수 없거나 프레임과 해상도가 일치하지 않으면 프레임을 제거한다.

일반적인 태블릿·휴대폰 목업이나 타사 기기 프레임을 사용하지 않는다.

스크린샷 크기

생성 시점에 Apple의 공식 Screenshot specifications와 App Store Connect의 View All Sizes in Media Manager를 확인한다.

오래된 고정 크기를 무조건 재사용하지 않는다.

현재 대상 슬롯이 요구하는 허용 크기 중 실제 캡처 기기와 일치하는 크기를 선택한다.

앱이 iPad를 지원하면 실제 iPad 결과물을 제공한다.

최종 이미지에 알파 채널이나 투명도를 포함하지 않는다.

현재 사양 검증에는 scripts/preflight_assets.py를 사용하되, Apple 공식 문서가 변경되면 문서를 우선한다.

카메라 권한 사전 점검

스크린샷 생성 전에 앱의 카메라 권한 흐름도 확인한다.

권한이 필요한 기능을 사용하려는 시점에 시스템 권한 요청을 연다.

시스템 권한 알림 전에 설명 화면이 필요하면 버튼은 하나만 둔다.

그 버튼은 시스템 권한 알림을 열어야 한다.

버튼 문구는 언어별 계속, 다음, Continue, Next처럼 중립적으로 작성한다.

카메라 접근 허용, Allow camera access, 허용처럼 사용자의 선택을 미리 유도하는 문구를 사용하지 않는다.

시스템 권한 알림을 모방한 이미지, “허용을 누르세요” 안내, 화살표·강조 표시를 사용하지 않는다.

이 흐름이 위 조건을 충족하지 않으면 스크린샷 제작을 중단하고 앱 수정 필요 사항을 보고한다.

권한 안내 화면은 핵심 기능을 보여주는 6장에 기본적으로 포함하지 않는다.

핵심 화면별 조건

1. 자동 카운트

실제 운동 중 카운트가 보이는 화면을 사용한다.

현재 세트, 현재 횟수, 누적 횟수 또는 상태가 실제로 보이면 활용한다.

사람과 MediaPipe 포즈 라인은 앱이 실제로 렌더링한 동일 프레임이어야 한다.

2. 실시간 자세 또는 음성 코칭

실제 피드백 UI가 보이는 화면을 사용한다.

“음성”을 쓰려면 현재 빌드에서 실제 음성 출력이 발생하는 것을 확인한다.

사람만 있거나 별도 제작한 포즈 라인만 있는 이미지는 사용하지 않는다.

3. 챌린지

실제 챌린지 탭과 목표, 진행률, 남은 기간 중 구현된 정보를 보여준다.

챌린지가 없거나 비활성화돼 있으면 다른 반복 사용 기능으로 교체한다.

4. 세트와 휴식

실제 휴식 타이머, 다음 세트, 완료 세트 수 중 구현된 정보를 보여준다.

5. 운동 결과

실제 완료 화면의 총 횟수, 세트 수, 시간, 자세 요약 중 구현된 정보를 보여준다.

6. 기록

실제 달력, 일별 합계, 세션 요약 중 구현된 정보를 보여준다.

현지화

단순 직역하지 말고 각 언어에서 짧고 자연스럽게 현지화한다.

독일어, 프랑스어, 스페인어는 길이를 줄인다.

일본어는 설명형 장문을 피한다.

한국어는 짧은 판매형 문구를 쓴다.

아랍어는 제목·부제목과 앱 자체가 지원하는 범위에서 RTL을 검수한다.

앱 내부 UI가 해당 언어 또는 RTL을 실제로 지원하지 않으면 번역된 UI를 합성하지 않는다. 지원되는 실제 앱 언어만 캡처하거나 미지원 사실을 보고한다.

최종 QA 게이트

각 결과물에 대해 다음을 모두 확인한다.

기능 주장과 evidence.md 근거가 일치한다.

현재 빌드에서 직접 캡처한 원본을 사용했다.

iPhone과 iPad 원본이 실제로 다르다.

앱 UI, 수치, 사람, 포즈 라인을 생성하거나 수정하지 않았다.

상태 표시줄이 실제 동일 플랫폼 캡처이거나 완전히 보이지 않는다.

다른 모바일 플랫폼의 이미지가 없다.

기기 프레임이 없거나 정확한 모델·권한이 확인됐다.

헤드라인이 1~2줄이고 축소해도 읽힌다.

부제목이 0~1줄이다.

파일 크기와 픽셀 크기가 대상 슬롯 사양에 맞는다.

알파 채널과 투명도가 없다.

개인정보와 실제 개인 계정 정보가 노출되지 않는다.

카메라 권한 사전 화면이 Apple 지침을 위반하지 않는다.

하나라도 실패하면 최종 폴더에 넣지 않는다. 실패 이유와 필요한 실제 캡처 또는 앱 수정 사항을 qa-report.md에 적는다.

실행 프롬프트

실제 작업을 시작할 때는 references/screenshot-run-prompt.md의 프롬프트를 사용한다.